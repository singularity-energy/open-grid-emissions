name: hourly_egrid
channels:
  - defaults
  - conda-forge
dependencies:
  - dask
  - ipykernel
  - matplotlib
  - missingno
  - notebook
  - numpy
  - openpyxl
  - pandas
  - pathlib
  - pip
  - plotly
  - pyarrow
  - python
  - requests
  - sqlalchemy
  - statsmodels
  - tabulate


  - setuptools # used for pudl
  - geopandas # used for pudl
  - pygeos  # used for pudl
  - python-snappy # used for pudl
  - sqlite # used for pudl

  - pip:
    #- --editable ../pudl #NOTE: this is for Greg's use for development
    - git+git://github.com/grgmiller/pudl.git@allocate_fuel#egg=catalystcoop.pudl

